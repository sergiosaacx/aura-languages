{
  "name": "aura-stream-api",
  "version": "3.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "pip3 install -U yt-dlp || pip install -U yt-dlp || true",
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
